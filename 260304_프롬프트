"Generate a technical architecture diagram of the NISQA speech quality assessment model in academic paper style. Include these elements:  

1. **Input Layer**:   
   - Raw audio waveform with sample rate label (e.g., 16kHz)  
   - Spectrogram conversion block (FFT-based)  

2. **Feature Extraction Path**:  
   - 2D Convolutional Neural Network (CNN) block with 3 layers  
   - Batch normalization and ReLU activation indicators  
   - Channel reduction arrow (e.g., 128 → 64 channels)  

3. **Temporal Modeling Path**:  
   - Transformer Encoder block with multi-head attention  
   - Positional encoding input  
   - Layer normalization indicators  

4. **Fusion Mechanism**:  
   - Cross-attention between CNN features and Transformer outputs  
   - Feature concatenation point  

5. **Output Layer**:  
   - Fully connected layer with sigmoid activation  
   - MOS score output (0-5 scale) with regression calibration  

6. **Visual Requirements**:  
   - Clean lines with academic sans-serif font  
   - Color-coded components (blue for input, green for CNN, orange for Transformer)  
   - Arrows showing data flow direction  
   - Block labels with technical terms in English  
   - Grid background for alignment  
   - Size: A4 paper width, height proportional to content"  
